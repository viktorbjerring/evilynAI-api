scheduler-tool:
  tool_state:
    - type: object
      properties:
        enabled:
          type: boolean
        description:
          type: string
        num_active_schedules:
          type: integer
        active_schedule_ids:
          type: array
          items:
            type: string

  details:
    - type: object
      properties:
        timezone:
          type: string
        active_schedules:
          type: integer
        notify_notice:
          type: string
